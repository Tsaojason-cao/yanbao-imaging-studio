# 🏗️ yanbao AI 工业级进化指令：从"修图工具"到"智能影像工作室"

**文档版本**：v2.0 - 架构师级硬指令  
**发布日期**：2026年1月13日  
**目标受众**：Manus AI 开发团队  
**执行期限**：20周（5个月）  
**成功指标**：全面超越『像素蛋糕』与『美图秀秀』

---

## 🎯 总体目标

将 **yanbao AI** 从基础修图工具升级为**工业级智能影像工作室**，通过五大核心功能的实现，建立**不可复制的竞争壁垒**。

| 阶段 | 功能 | 工作量 | 难度 | 交付周期 |
|------|------|--------|------|---------|
| 1 | AI 消除（LAMA） | 4周 | ⭐⭐⭐⭐ | 第1-4周 |
| 2 | 视频美颜（GLSL） | 4周 | ⭐⭐⭐⭐⭐ | 第5-8周 |
| 3 | 批量处理（Batch） | 2周 | ⭐⭐⭐ | 第9-10周 |
| 4 | AR 姿势引导 | 6周 | ⭐⭐⭐⭐⭐ | 第11-16周 |
| 5 | AI 扩图（Outpainting） | 4周 | ⭐⭐⭐⭐ | 第17-20周 |

---

# 🟢 第一阶段：AI 消除（LAMA Inpainting）

**Milestone 1.1**：后端 LAMA 推理引擎  
**Milestone 1.2**：前端涂鸦遮罩 UI  
**Milestone 1.3**：端到端集成与测试

## 1.1 后端技术栈约束

### 框架与依赖
```
FastAPI 0.104.1+
Python 3.10+
PyTorch 2.0+ (CUDA 11.8)
LAMA (LaMa-Cleaner)
OpenCV 4.8+
Pillow 10.0+
aiofiles 23.2+
python-multipart 0.0.6+
```

### 硬性要求

#### ❌ 拒绝的实现方式
- ❌ 调用 Remove.bg API（费用高、依赖外网、响应慢）
- ❌ 使用 Rembg 库（精度不足、效果廉价）
- ❌ 简单的图像处理库（OpenCV inpaint、Photoshop 算法）
- ❌ Mock 数据或占位符代码

#### ✅ 必须实现的方式
- ✅ **本地 LAMA 模型推理**（GPU 加速）
- ✅ **支持 4K 图片**（最大 4096x4096）
- ✅ **异步处理队列**（FastAPI asyncio）
- ✅ **WebSocket 实时进度推送**
- ✅ **Refinement 步数 ≥ 25**（保证质量）
- ✅ **内存占用 < 500MB**（单任务）

### 关键技术指标

| 指标 | 要求 | 验收标准 |
|------|------|---------|
| 1080p 处理时间 | < 5秒 | 实测 |
| 4K 处理时间 | < 15秒 | 实测 |
| GPU 内存占用 | < 500MB | nvidia-smi |
| 并发 Worker | 4个 | 支持 CUDA 多进程 |
| 队列大小 | 50 | 防止 OOM |
| Refinement 步数 | ≥ 25 | 质量保证 |
| 掩码精度 | > 95% | 二值化阈值 128 |

### 后端 API 规范

**POST /api/v1/inpaint**
```json
请求：
{
  "image": "multipart/form-data (JPEG/PNG)",
  "mask": "multipart/form-data (PNG, 白色=移除区域)",
  "priority": 0-2,
  "refinement_steps": 25
}

响应：
{
  "success": true,
  "taskId": "uuid",
  "status": "queued",
  "wsUrl": "/ws/inpaint/{taskId}"
}
```

**GET /api/v1/inpaint/{taskId}**
```json
响应：
{
  "success": true,
  "taskId": "uuid",
  "status": "processing|completed|failed",
  "progress": 0-100,
  "result": "/api/v1/inpaint/{taskId}/result"
}
```

**WebSocket /ws/inpaint/{taskId}**
```json
消息格式：
{
  "type": "progress|completed|error",
  "progress": 0-100,
  "status": "processing|completed|failed"
}
```

### 环境变量配置

**`.env` 文件**
```bash
# LAMA 模型配置
LAMA_DEVICE=cuda:0
LAMA_CHECKPOINT_PATH=/models/lama-mpe.ckpt
LAMA_MAX_IMAGE_SIZE=4096
LAMA_REFINEMENT_STEPS=25
LAMA_BATCH_SIZE=1
LAMA_NUM_WORKERS=4

# GPU 配置
CUDA_VISIBLE_DEVICES=0
TORCH_HOME=/models/torch_cache

# 服务配置
INPAINT_SERVICE_PORT=8000
INPAINT_WORKERS=4
INPAINT_QUEUE_SIZE=50

# 文件配置
UPLOAD_DIR=/tmp/uploads
RESULT_DIR=/tmp/results
```

---

## 1.2 前端技术栈约束

### 框架与依赖
```
React Native 0.73+
Expo 50.0+
@shopify/react-native-skia 0.1.200+
expo-camera 14.0+
expo-av 13.10+
axios 1.6+
```

### UI 组件规范

**涂鸦遮罩组件**
- 使用 **Skia Canvas**（不是 WebGL）
- 支持笔刷大小：10px - 100px
- 支持撤销/重做：最多 50 步
- 实时笔迹预览：< 16ms 延迟
- 掩码生成：黑色背景 + 白色笔迹

**关键代码片段**
```typescript
// 掩码生成规范
const generateMask = async (): Promise<Blob> => {
  const canvas = document.createElement('canvas');
  const ctx = canvas.getContext('2d');
  
  // 背景必须是纯黑色 (#000000)
  ctx.fillStyle = '#000000';
  ctx.fillRect(0, 0, width, height);
  
  // 笔迹必须是纯白色 (#FFFFFF)
  ctx.strokeStyle = '#FFFFFF';
  ctx.lineWidth = brushSize;
  ctx.lineCap = 'round';
  ctx.lineJoin = 'round';
  
  // 绘制所有路径
  paths.forEach(({ points }) => {
    ctx.beginPath();
    ctx.moveTo(points[0].x, points[0].y);
    points.forEach(p => ctx.lineTo(p.x, p.y));
    ctx.stroke();
  });
  
  return canvas.toBlob(blob => blob, 'image/png');
};
```

### 用户交互流程

1. **加载图片** → 显示在 Skia Canvas 上
2. **涂鸦标记** → 实时显示红色笔迹
3. **撤销/重做** → 支持 50 步历史
4. **生成掩码** → 转换为黑白二值图
5. **上传任务** → POST /api/v1/inpaint
6. **WebSocket 连接** → 实时接收进度
7. **进度显示** → 进度条 0% → 100%
8. **结果预览** → 显示移除后的图片

---

## 1.3 交付与验收

### Milestone 1.1：后端 LAMA 推理引擎（第2周末）

**验收清单**
- [ ] LAMA 模型成功加载到 GPU
- [ ] 单张 1080p 图片处理 < 5秒
- [ ] 支持 4K 图片处理 < 15秒
- [ ] GPU 内存占用 < 500MB
- [ ] 异步队列支持 4 个 worker
- [ ] WebSocket 实时推送进度
- [ ] 错误处理完整（超时、OOM、无效输入）
- [ ] 所有 API 端点正常工作

**质询清单**（用于验证 Manus 的实现）
- "LAMA 模型的 refinement 步数设置的是多少？"
  - **正确答案**：≥ 25（保证质量）
- "是否支持 4K 图片？最大尺寸是多少？"
  - **正确答案**：支持，最大 4096x4096
- "GPU 内存占用是否超过 500MB？"
  - **正确答案**：不超过，使用 nvidia-smi 验证
- "队列满了怎么处理？"
  - **正确答案**：返回 503 Service Unavailable

### Milestone 1.2：前端涂鸦遮罩 UI（第3周末）

**验收清单**
- [ ] Skia Canvas 实时笔迹显示
- [ ] 笔刷大小可调（10-100px）
- [ ] 撤销/重做功能正常
- [ ] 掩码生成正确（黑色背景 + 白色笔迹）
- [ ] WebSocket 连接成功
- [ ] 进度条实时更新
- [ ] 结果图片正确显示

**质询清单**
- "掩码的黑色和白色值是多少？"
  - **正确答案**：#000000（黑）和 #FFFFFF（白）
- "笔迹延迟是多少？"
  - **正确答案**：< 16ms（60fps）
- "支持多少步撤销？"
  - **正确答案**：50 步

### Milestone 1.3：端到端集成（第4周末）

**验收清单**
- [ ] 完整的端到端流程正常
- [ ] 图片质量满足预期
- [ ] 性能指标达到目标
- [ ] 代码已推送到 GitHub
- [ ] 测试包已生成（eas build）

**GitHub Commit 规范**
```bash
git commit -m "[Feature-Stage 1] Implement LAMA-based AI inpainting

- Backend: FastAPI + LAMA model + async queue + WebSocket
- Frontend: Skia Canvas doodle component + mask generation
- Performance: 1080p < 5s, 4K < 15s, GPU < 500MB
- Validation: All API endpoints tested and working"
```

---

# 🟢 第二阶段：视频拍摄与实时美颜（GLSL Shaders）

**Milestone 2.1**：GLSL 着色器实现  
**Milestone 2.2**：视频录制与编辑  
**Milestone 2.3**：实时美颜集成

## 2.1 技术栈约束

### 框架与依赖
```
expo-gl 13.0+
expo-camera 14.0+
expo-av 13.10+
expo-video-effector (或 react-native-ffmpeg)
GLSL 3.0 ES
OpenGL 2.0+
```

### GLSL 着色器规范

#### 顶点着色器（Vertex Shader）
```glsl
#version 300 es

in vec4 position;
in vec2 texCoord;

out vec2 vTexCoord;

uniform mat4 uProjectionMatrix;

void main() {
  gl_Position = uProjectionMatrix * position;
  vTexCoord = texCoord;
}
```

#### 片段着色器（Fragment Shader）- 核心美颜逻辑

**硬性要求**
- ✅ YUV 420 → RGB 转换
- ✅ 磨皮（高斯模糊 + 融合）
- ✅ 瘦脸（网格变形）
- ✅ 亮眼（局部增强）
- ✅ 口红（色彩替换）
- ✅ 皮肤色域遮罩（不改变背景）
- ✅ 色彩校正（对比度、饱和度）

**性能指标**
| 指标 | 要求 | 验收标准 |
|------|------|---------|
| 实时预览 FPS | 60fps | 1080p 实时 |
| 单帧处理时间 | < 16.7ms | 60fps = 16.7ms/帧 |
| GPU 占用率 | < 80% | nvidia-smi |
| 内存占用 | < 300MB | 不影响其他进程 |

### 关键 GLSL 代码片段

**皮肤检测与遮罩**
```glsl
bool isSkinPixel(vec3 rgb) {
  // 基于 RGB 范围的皮肤检测
  float r = rgb.r;
  float g = rgb.g;
  float b = rgb.b;
  
  return (r > 95.0 && g > 40.0 && b > 20.0 &&
          r > g && r > b &&
          abs(r - g) > 15.0);
}

// 只对皮肤区域应用美颜，不改变背景
if (isSkinPixel(rgb)) {
  rgb = applyBeautyEffects(rgb);
}
```

**磨皮（高斯模糊）**
```glsl
vec3 gaussianBlur(sampler2D tex, vec2 uv, float radius) {
  vec3 result = vec3(0.0);
  float weights = 0.0;
  
  // 9 点高斯模糊
  float offset[3] = float[](0.0, 1.3846153846, 3.2307692308);
  float weight[3] = float[](0.2270270270, 0.3162162162, 0.0702702703);
  
  for(int i = 0; i < 3; i++) {
    for(int j = 0; j < 3; j++) {
      vec2 offset_uv = uv + vec2(offset[i], offset[j]) * radius;
      float y = texture(tex, offset_uv).r;
      result += vec3(y) * weight[i] * weight[j];
      weights += weight[i] * weight[j];
    }
  }
  
  return result / weights;
}
```

### 质询清单（验证 Manus 的实现）

- "GLSL 代码里是否处理了皮肤色域的遮罩？"
  - **正确答案**：是，使用 isSkinPixel() 函数检测
- "不要让滤镜改变了背景的颜色。"
  - **正确答案**：只对皮肤区域应用美颜，背景保持原色
- "磨皮的高斯模糊半径是多少？"
  - **正确答案**：0.005（相对于纹理坐标）
- "瘦脸的变形强度是多少？"
  - **正确答案**：0.0 - 1.0（可调）

---

## 2.2 视频录制与编辑

### 功能要求
- ✅ 1080p 或 4K 录制
- ✅ 实时美颜预览（60fps）
- ✅ 视频裁剪（起始帧、结束帧）
- ✅ 变速（0.5x - 2.0x）
- ✅ LUT 滤镜应用
- ✅ 导出为 MP4（H.264 编码）

### 视频编码规范
```
编码格式：H.264
分辨率：1080p (1920x1080) 或 4K (3840x2160)
帧率：30fps 或 60fps
比特率：2-5 Mbps（1080p）
音频：AAC, 128kbps
容器：MP4
```

---

## 2.3 交付与验收

### Milestone 2.1：GLSL 着色器实现（第6周末）

**验收清单**
- [ ] 顶点着色器编译无错误
- [ ] 片段着色器编译无错误
- [ ] YUV to RGB 转换正确
- [ ] 磨皮效果自然（不过度）
- [ ] 瘦脸效果明显（可调）
- [ ] 亮眼效果不过度
- [ ] 口红颜色准确
- [ ] 皮肤色域遮罩有效（背景不改变）

### Milestone 2.2：视频录制与编辑（第7周末）

**验收清单**
- [ ] 1080p 实时预览 60fps
- [ ] 视频录制正常
- [ ] 裁剪功能正常
- [ ] 变速功能正常（0.5x - 2.0x）
- [ ] LUT 滤镜应用正常

### Milestone 2.3：实时美颜集成（第8周末）

**验收清单**
- [ ] 录制时实时应用美颜
- [ ] 导出视频包含美颜效果
- [ ] 性能指标达到目标（60fps）
- [ ] 代码已推送到 GitHub

**GitHub Commit 规范**
```bash
git commit -m "[Feature-Stage 2] Implement real-time video beauty with GLSL

- Backend: GLSL shaders for skin smoothing, face slimming, eye brightening
- Frontend: expo-gl + expo-camera integration
- Performance: 60fps real-time preview, GPU < 80%
- Features: Video recording, editing, LUT filters
- Validation: All beauty effects tested on real devices"
```

---

# 🟢 第三阶段：批量处理与生产力（Batch Engine）

**Milestone 3.1**：多选 UI 与 FlashList  
**Milestone 3.2**：后端批量 API  
**Milestone 3.3**：集成与测试

## 3.1 技术栈约束

### 框架与依赖
```
@shopify/flash-list 1.6+
React Native 0.73+
FastAPI 0.104.1+
```

### 性能指标

| 指标 | 要求 | 验收标准 |
|------|------|---------|
| 多选 50 张照片 | 60fps | FlashList 虚拟滚动 |
| 批量处理 50 张 | < 30秒 | 后端异步处理 |
| 内存占用 | < 200MB | 不加载全部图片 |
| 进度更新 | 实时 | WebSocket 推送 |

### 前端 UI 规范

**多选模式**
- 长按照片进入多选模式
- 支持全选/取消全选
- 显示已选择数量
- 支持单个取消选择

**预设应用**
- 显示可用预设列表
- 点击预设应用到所有选中照片
- 显示处理进度条
- 支持取消操作

### 后端 API 规范

**POST /api/v1/batch/apply-preset**
```json
请求：
{
  "photoIds": ["id1", "id2", ...],
  "presetId": "preset-uuid",
  "userId": "user-uuid"
}

响应：
{
  "success": true,
  "processedCount": 50,
  "totalCount": 50,
  "results": [...]
}
```

---

## 3.2 交付与验收

### Milestone 3.1：多选 UI（第9周末）

**验收清单**
- [ ] FlashList 虚拟滚动正常
- [ ] 多选 50+ 张照片时 60fps
- [ ] 全选/取消全选功能正常
- [ ] 显示已选择数量

### Milestone 3.2：后端批量 API（第9周末）

**验收清单**
- [ ] 批量应用预设 API 正常
- [ ] 支持 50 张并发处理
- [ ] 处理时间 < 30秒
- [ ] 进度实时推送

### Milestone 3.3：集成与测试（第10周末）

**验收清单**
- [ ] 端到端流程正常
- [ ] 性能指标达到目标
- [ ] 代码已推送到 GitHub

**GitHub Commit 规范**
```bash
git commit -m "[Feature-Stage 3] Implement batch processing engine

- Frontend: FlashList multi-select UI, 60fps with 50+ photos
- Backend: Batch preset application API, async processing
- Performance: < 30s for 50 photos, < 200MB memory
- Validation: End-to-end batch workflow tested"
```

---

# 🟢 第四阶段：AR 姿势引导（MediaPipe AR）

**Milestone 4.1**：MediaPipe Pose 集成  
**Milestone 4.2**：姿势检测与匹配  
**Milestone 4.3**：姿势模板库

## 4.1 技术栈约束

### 框架与依赖
```
@tensorflow/tfjs 4.10+
@tensorflow-models/pose-detection 2.2+
MediaPipe Pose
@shopify/react-native-skia 0.1.200+
```

### 性能指标

| 指标 | 要求 | 验收标准 |
|------|------|---------|
| 实时检测 FPS | 30fps | 1080p 实时 |
| 关键点精度 | > 85% | 与模板匹配度 |
| 内存占用 | < 200MB | 模型 + 推理 |
| 延迟 | < 33ms | 30fps = 33ms/帧 |

### 关键点规范

**33 个关键点**（MediaPipe Pose）
- 0-10：头部（鼻子、眼睛、耳朵）
- 11-16：上肢（肩膀、肘、手腕）
- 17-22：躯干（腰、臀）
- 23-28：下肢（膝、脚踝）
- 29-32：脚趾

### 骨架连接

```
肩膀：11-12
左臂：11-13-15
右臂：12-14-16
躯干：11-23, 12-24
腰部：23-24
左腿：23-25-27
右腿：24-26-28
```

### 姿势匹配度计算

```
匹配度 = Σ(1 - 欧氏距离) / 关键点数
如果匹配度 > 85%，快门按钮变色提示
```

### 质询清单

- "MediaPipe Pose 支持多少个关键点？"
  - **正确答案**：33 个
- "姿势匹配度阈值是多少？"
  - **正确答案**：> 85%（可调）
- "实时检测的 FPS 是多少？"
  - **正确答案**：30fps（1080p）

---

## 4.2 姿势模板库

### 内置 20 组模板

**可颂风格**（10 组）
1. 站立正面
2. 站立侧面
3. 站立 45 度
4. 坐姿
5. 躺姿
6. 蹲姿
7. 跳跃
8. 转身
9. 靠墙
10. 靠树

**库洛米风格**（10 组）
1. 可爱站姿
2. 俏皮坐姿
3. 卖萌蹲姿
4. 跳跃欢呼
5. 靠肩膀
6. 双手比心
7. 单手指向
8. 躺地板
9. 趴地板
10. 转身回眸

### 每个模板包含

- 模板 ID
- 模板名称
- 33 个关键点坐标
- 拍摄提示文案（中文）
- 参考图片

---

## 4.3 交付与验收

### Milestone 4.1：MediaPipe 集成（第12周末）

**验收清单**
- [ ] MediaPipe Pose 模型加载正常
- [ ] 33 个关键点检测准确
- [ ] 实时检测 30fps
- [ ] 内存占用 < 200MB

### Milestone 4.2：姿势检测与匹配（第14周末）

**验收清单**
- [ ] 姿势匹配度计算正确
- [ ] 匹配度 > 85% 时快门按钮变色
- [ ] 骨架绘制正确
- [ ] 实时反馈延迟 < 33ms

### Milestone 4.3：姿势模板库（第16周末）

**验收清单**
- [ ] 20 组姿势模板完整
- [ ] 每个模板包含参考图片
- [ ] 拍摄提示文案为中文
- [ ] 用户可以选择模板
- [ ] 代码已推送到 GitHub

**GitHub Commit 规范**
```bash
git commit -m "[Feature-Stage 4] Implement AR pose guidance with MediaPipe

- Backend: MediaPipe Pose integration, 33 keypoints detection
- Frontend: Real-time skeleton drawing, pose matching (>85%)
- Features: 20 pose templates (Croissant + Kuromis style)
- Performance: 30fps real-time detection, < 200MB memory
- Validation: Pose guidance tested on real devices"
```

---

# 🟢 第五阶段：AI 扩图（Outpainting / Generative Fill）

**Milestone 5.1**：Stable Diffusion 集成  
**Milestone 5.2**：扩图 UI 与交互  
**Milestone 5.3**：多方向扩展

## 5.1 技术栈约束

### 框架与依赖
```
Stable Diffusion Inpainting
FastAPI 0.104.1+
PyTorch 2.0+
CUDA 11.8+
ControlNet (可选，用于更精确的扩展)
```

### 性能指标

| 指标 | 要求 | 验收标准 |
|------|------|---------|
| 1024x1024 生成时间 | < 10秒 | GPU 推理 |
| 2048x2048 生成时间 | < 30秒 | GPU 推理 |
| GPU 内存占用 | < 1GB | 单任务 |
| 扩展方向 | 4 个 | 上下左右 |

### 扩图比例

| 比例 | 尺寸 | 用途 |
|------|------|------|
| 1:1 | 1024x1024 | 方形 |
| 16:9 | 1920x1080 | 横屏 |
| 9:16 | 1080x1920 | 竖屏 |
| 4:3 | 1024x768 | 传统 |
| 3:4 | 768x1024 | 传统竖屏 |

### 扩展方向

```
原图（中央）
    ↓
上扩展：增加上方内容
下扩展：增加下方内容
左扩展：增加左方内容
右扩展：增加右方内容
四向扩展：同时扩展四个方向
```

### 提示词优化

```python
# 自动生成提示词
def generate_prompt(original_image, direction):
    # 分析原图内容
    # 根据扩展方向生成合适的提示词
    # 例如：
    # 原图：人物特写
    # 方向：下扩展
    # 提示词："full body shot, same person, natural pose"
```

---

## 5.2 后端 API 规范

**POST /api/v1/expand**
```json
请求：
{
  "image": "multipart/form-data (JPEG/PNG)",
  "direction": "up|down|left|right|all",
  "ratio": "1:1|16:9|9:16|4:3|3:4",
  "prompt": "optional custom prompt"
}

响应：
{
  "success": true,
  "taskId": "uuid",
  "status": "queued",
  "wsUrl": "/ws/expand/{taskId}"
}
```

---

## 5.3 前端 UI 规范

**扩图编辑器**
- 显示原图
- 显示四个方向的扩展预览
- 支持拖动边缘进行扩展
- 显示扩展进度
- 支持取消操作

---

## 5.4 交付与验收

### Milestone 5.1：Stable Diffusion 集成（第18周末）

**验收清单**
- [ ] Stable Diffusion 模型加载正常
- [ ] 1024x1024 生成 < 10秒
- [ ] 2048x2048 生成 < 30秒
- [ ] GPU 内存占用 < 1GB

### Milestone 5.2：扩图 UI（第19周末）

**验收清单**
- [ ] 扩图编辑器 UI 正常
- [ ] 四个方向扩展预览正常
- [ ] 拖动边缘扩展正常
- [ ] 进度显示正常

### Milestone 5.3：多方向扩展（第20周末）

**验收清单**
- [ ] 单向扩展正常
- [ ] 多向扩展正常
- [ ] 提示词自动生成正常
- [ ] 代码已推送到 GitHub

**GitHub Commit 规范**
```bash
git commit -m "[Feature-Stage 5] Implement AI image expansion (Outpainting)

- Backend: Stable Diffusion Inpainting integration
- Frontend: Image expansion editor UI, 4-direction support
- Features: Multiple aspect ratios, auto prompt generation
- Performance: 1024x1024 < 10s, 2048x2048 < 30s
- Validation: End-to-end outpainting workflow tested"
```

---

# 🛠️ 全局执行规范（必读）

## 代码同步规范

### GitHub Commit 规范
```bash
# 格式
git commit -m "[Feature-Stage X] <功能描述>

<详细说明>
- 技术栈：...
- 性能指标：...
- 验收标准：...
"

# 示例
git commit -m "[Feature-Stage 1] Implement LAMA-based AI inpainting

- Backend: FastAPI + LAMA model + async queue + WebSocket
- Frontend: Skia Canvas doodle component + mask generation
- Performance: 1080p < 5s, 4K < 15s, GPU < 500MB
- Validation: All API endpoints tested and working"
```

### 分支管理
```bash
# 主分支：main
# 开发分支：feature/stage-X

git checkout -b feature/stage-1
# 开发...
git commit -m "[Feature-Stage 1] ..."
git push origin feature/stage-1
git pull request (merge to main)
```

## 环境变量规范

### 禁止事项
- ❌ 明文写在代码里
- ❌ 提交到 Git
- ❌ 硬编码 API Key

### 要求
- ✅ 存放在 `.env` 文件
- ✅ `.env` 添加到 `.gitignore`
- ✅ 提供 `.env.example` 模板
- ✅ 文档中说明所有环境变量

### 示例
```bash
# .env
LAMA_DEVICE=cuda:0
LAMA_CHECKPOINT_PATH=/models/lama-mpe.ckpt
OPENAI_API_KEY=sk-...
STRIPE_API_KEY=sk_live_...

# .env.example
LAMA_DEVICE=cuda:0
LAMA_CHECKPOINT_PATH=/models/lama-mpe.ckpt
OPENAI_API_KEY=your_api_key_here
STRIPE_API_KEY=your_stripe_key_here
```

## 实机交付规范

### EAS Build 命令
```bash
# 开发包（用于测试）
eas build --platform android --profile development

# 生产包（用于发布）
eas build --platform android --profile production

# 获取直链
# 构建完成后，从 EAS 控制面板获取 APK 直链
```

### 交付清单
- [ ] 开发包 APK 已生成
- [ ] 直链已获取
- [ ] 功能在真实设备上验证
- [ ] 性能指标已测试

## 中文文案规范

### App 名称
- **正式名称**：yanbao AI
- **副标题**：智能影像工作室

### 交互文案（所有使用简体中文）

| 功能 | 文案 |
|------|------|
| AI 消除 | "AI 消除中..." |
| 视频美颜 | "正在应用美颜..." |
| 批量处理 | "批量处理中..." |
| 姿势引导 | "检测姿势中..." |
| AI 扩图 | "正在扩图..." |
| 完成 | "完成！" |
| 错误 | "处理失败，请重试" |

---

# 📊 进度追踪表

| 阶段 | 功能 | 开始周 | 结束周 | 状态 | GitHub Commit |
|------|------|--------|--------|------|---------------|
| 1 | AI 消除 | 1 | 4 | ⏳ | - |
| 2 | 视频美颜 | 5 | 8 | ⏳ | - |
| 3 | 批量处理 | 9 | 10 | ⏳ | - |
| 4 | AR 姿势 | 11 | 16 | ⏳ | - |
| 5 | AI 扩图 | 17 | 20 | ⏳ | - |

---

# 🎯 成功指标

## 功能完整性
- ✅ 五大核心功能全部实现
- ✅ 性能指标全部达到目标
- ✅ 代码质量达到工业级

## 竞争力对标
- ✅ 超越『像素蛋糕』的 AI 消除
- ✅ 超越『美颜相机』的实时美颜
- ✅ 超越『可颂』的批量处理
- ✅ 超越『美图秀秀』的 AR 功能
- ✅ 独有的 AI 扩图功能

## 用户体验
- ✅ 中文交互文案完整
- ✅ 性能流畅（60fps）
- ✅ 功能易用（新手友好）

---

**发布日期**：2026年1月13日  
**架构师**：Manus AI 开发团队  
**最后更新**：2026年1月13日

---

## 附录：质询清单（用于监督进度）

### 第一阶段质询
1. "LAMA 模型的 refinement 步数设置的是多少？"
2. "是否支持 4K 图片？最大尺寸是多少？"
3. "GPU 内存占用是否超过 500MB？"
4. "队列满了怎么处理？"
5. "掩码的黑色和白色值是多少？"

### 第二阶段质询
1. "GLSL 代码里是否处理了皮肤色域的遮罩？"
2. "不要让滤镜改变了背景的颜色。"
3. "磨皮的高斯模糊半径是多少？"
4. "瘦脸的变形强度是多少？"
5. "实时预览的 FPS 是多少？"

### 第三阶段质询
1. "FlashList 虚拟滚动是否支持 60fps？"
2. "批量处理 50 张照片需要多长时间？"
3. "内存占用是否超过 200MB？"

### 第四阶段质询
1. "MediaPipe Pose 支持多少个关键点？"
2. "姿势匹配度阈值是多少？"
3. "实时检测的 FPS 是多少？"
4. "内置多少组姿势模板？"

### 第五阶段质询
1. "1024x1024 图片生成需要多长时间？"
2. "支持多少个扩展方向？"
3. "GPU 内存占用是否超过 1GB？"

