# yanbao AI v2.4.1 - 本地构建环境检查清单

在执行一键构建脚本前，请确保您的本地环境满足以下所有要求。

---

## ✅ 核心环境要求

| 检查项 | 要求 | 如何检查 | 状态 |
| :--- | :--- | :--- | :--- |
| **操作系统** | macOS / Linux / Windows (WSL2) | `uname -a` | ⬜ |
| **Node.js** | 22.13.0 | `node -v` | ⬜ |
| **pnpm** | >= 8.0.0 | `pnpm -v` | ⬜ |
| **EAS CLI** | >= 16.0.0 | `eas --version` | ⬜ |
| **Git** | >= 2.0.0 | `git --version` | ⬜ |
| **Expo 账号** | 已注册并登录 | `eas whoami` | ⬜ |

---

## ✅ Android 构建环境（如果使用本地构建）

| 检查项 | 要求 | 如何检查 | 状态 |
| :--- | :--- | :--- | :--- |
| **Java JDK** | 17 | `java -version` | ⬜ |
| **Android Studio** | 最新版本 | 打开 Android Studio | ⬜ |
| **Android SDK** | API Level 35 | SDK Manager | ⬜ |
| **SDK Build-Tools** | 34.0.0 | SDK Manager | ⬜ |
| **NDK** | 26.1.10909125 | SDK Manager | ⬜ |
| **环境变量** | `ANDROID_HOME` 已设置 | `echo $ANDROID_HOME` | ⬜ |
| **环境变量** | `JAVA_HOME` 已设置 | `echo $JAVA_HOME` | ⬜ |

---

## ✅ 项目配置检查

| 检查项 | 预期状态 | 如何检查 | 状态 |
| :--- | :--- | :--- | :--- |
| **代码同步** | 已拉取最新 `main` 分支 | `git status` | ⬜ |
| **依赖安装** | `node_modules` 目录存在 | `ls -d node_modules` | ⬜ |
| **Hermes 引擎** | 已在 `app.config.ts` 中启用 | `grep "hermes" app.config.ts` | ⬜ |
| **图片资产** | `assets/images` 目录大小约为 19MB | `du -sh assets/images` | ⬜ |

---

## 📋 检查步骤

### 1. 检查核心环境

打开您的终端，逐一执行以下命令，并与上表中的要求进行核对：

```bash
node -v
pnpm -v
eas --version
git --version
eas whoami
```

- 如果有任何命令未找到或版本不符，请先安装或升级。
- 如果 `eas whoami` 提示未登录，请执行 `eas login`。

### 2. 检查项目配置

进入 `yanbao-imaging-studio` 项目目录，执行以下命令：

```bash
# 1. 确认分支和更新状态
git status

# 2. 确认依赖已安装
ls -d node_modules

# 3. 确认 Hermes 引擎已启用
grep "hermes" app.config.ts

# 4. 确认图片资产已优化
du -sh assets/images
```

- 如果 `git status` 显示有未提交的修改，请先处理。
- 如果 `node_modules` 不存在，请运行 `pnpm install`。
- 如果 `grep` 命令没有输出，说明 Hermes 未启用，请检查 `app.config.ts`。
- 如果 `du` 显示的大小远大于 21MB，说明图片未优化，请检查代码版本。

---

## 💡 快速环境设置

### Node.js (使用 nvm)

```bash
nvm install 22.13.0
nvm use 22.13.0
```

### pnpm

```bash
npm install -g pnpm
```

### EAS CLI

```bash
npm install -g eas-cli
```

---

**当所有检查项都标记为 ✅ 时，您就可以执行 `build-apk.sh` 脚本了。**
