# Netlify 部署指南

## 📋 部署步骤

### 第 1 步：访问 Netlify

打开 [Netlify](https://app.netlify.com/)，使用 GitHub 账号登录。

---

### 第 2 步：导入项目

1. 点击 **"Add new site"** → **"Import an existing project"**
2. 选择 **"Deploy with GitHub"**
3. 授权 Netlify 访问您的 GitHub 账号
4. 在仓库列表中找到并选择 **`sanmu-imaging-studio`**

---

### 第 3 步：配置构建设置

Netlify 会自动检测到 `netlify.toml` 配置文件，但请确认以下设置：

| 配置项 | 值 |
|--------|-----|
| **Branch to deploy** | `main` |
| **Build command** | `pnpm build` |
| **Publish directory** | `dist/public` |
| **Node version** | `22` |

如果配置正确，直接点击 **"Deploy site"** 按钮。

---

### 第 4 步：等待部署完成

- 初次部署需要 3-5 分钟
- 您可以在 **"Deploys"** 页面查看构建日志
- 部署成功后，您会获得一个 `*.netlify.app` 域名

---

## 🎉 部署后您将获得

- ✨ **永久访问网址**：`https://sanmu-imaging-studio.netlify.app`（可自定义）
- 🔒 **自动 HTTPS**：免费 SSL 证书
- 🌍 **全球 CDN**：快速访问
- 🔄 **自动部署**：每次推送代码自动更新
- 📱 **PWA 支持**：完整的渐进式 Web 应用功能
- 🎨 **预览部署**：每个 Pull Request 都有独立的预览 URL

---

## 🔧 配置说明

### netlify.toml 配置文件

项目根目录的 `netlify.toml` 文件包含了所有部署配置：

#### 构建配置
```toml
[build]
  command = "pnpm build"
  publish = "dist/public"
```

#### 路由重定向
- 支持 SPA 单页应用路由
- 所有路径都指向 `index.html`

#### 安全响应头
- X-Frame-Options
- Content-Security-Policy
- X-Content-Type-Options
- 等等

#### 缓存策略
- 静态资源：1 年缓存
- PWA 文件：不缓存，始终获取最新版本

---

## 🌐 自定义域名（可选）

部署成功后，您可以绑定自己的域名：

1. 进入 **Site settings** → **Domain management**
2. 点击 **"Add custom domain"**
3. 输入您的域名（如 `sanmu.com`）
4. 按照提示配置 DNS 记录

---

## 🔄 自动部署

Netlify 已配置为自动部署：

- **推送到 `main` 分支** → 自动部署到生产环境
- **创建 Pull Request** → 自动创建预览部署
- **合并 PR** → 自动部署到生产环境

---

## 📊 部署状态徽章

您可以在 README.md 中添加部署状态徽章：

```markdown
[![Netlify Status](https://api.netlify.com/api/v1/badges/YOUR-SITE-ID/deploy-status)](https://app.netlify.com/sites/YOUR-SITE-NAME/deploys)
```

---

## 🛠️ 常见问题

### Q: 构建失败怎么办？
A: 查看 Netlify 的构建日志，通常是依赖安装或构建命令问题。

### Q: 如何查看部署日志？
A: 进入 **Deploys** 页面，点击具体的部署记录查看详细日志。

### Q: 如何回滚到之前的版本？
A: 在 **Deploys** 页面找到之前的部署，点击 **"Publish deploy"**。

### Q: 如何设置环境变量？
A: 进入 **Site settings** → **Build & deploy** → **Environment**。

---

## 🎯 Netlify 的优势

1. **零配置部署**：自动检测项目类型
2. **免费 SSL**：自动配置 HTTPS
3. **全球 CDN**：快速访问
4. **原子部署**：部署失败不影响现有网站
5. **即时回滚**：一键回滚到任意版本
6. **预览部署**：每个 PR 都有独立预览
7. **表单处理**：内置表单提交功能
8. **函数支持**：支持 Serverless Functions
9. **分析功能**：内置访问统计
10. **免费额度**：个人项目完全免费

---

## 📞 需要帮助？

- [Netlify 文档](https://docs.netlify.com/)
- [Netlify 社区](https://answers.netlify.com/)
- [Netlify 状态页面](https://www.netlifystatus.com/)

---

**祝您部署顺利！🚀**
